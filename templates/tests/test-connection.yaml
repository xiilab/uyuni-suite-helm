apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uyuni-suite.fullname" . }}-test-connection"
  labels:
    {{- include "uyuni-suite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "uyuni-suite.fullname" . }}:{{ .Values.uyuni.core.service.port }}']
  restartPolicy: Never
