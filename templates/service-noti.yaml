apiVersion: v1
kind: Service
metadata:
  name: "{{ include "uyuni-suite.fullname" . }}-noti"
  labels:
    {{- include "uyuni-suite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.uyuni.noti.service.type }}
  ports:
    - port: {{ .Values.uyuni.noti.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: uyuni-suite-noti
